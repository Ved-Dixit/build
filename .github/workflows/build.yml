name: Build/Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux Dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libarchive-tools ruby-dev build-essential icnsutils graphicsmagick xz-utils libsecret-1-dev
          sudo gem install fpm

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: desktop-app/package.json

      - name: Install Dependencies
        working-directory: ./desktop-app
        run: |
          npm config set fetch-retries 10
          npm config set fetch-retry-mintimeout 60000
          npm config set fetch-retry-maxtimeout 300000
          npm config set maxsockets 3
          npm install

      - name: Build and Release
        working-directory: ./desktop-app
        run: npm run build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ELECTRON_BUILDER_HTTP_TIMEOUT: 600000
          ELECTRON_BUILDER_HTTP_RETRY_COUNT: 20
          ELECTRON_MIRROR: "https://npmmirror.com/mirrors/electron/"
